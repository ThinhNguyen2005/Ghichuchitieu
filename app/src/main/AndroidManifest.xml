<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Phase 1: KHÔNG xin bất kỳ permission nào.
         Phase 2 mới thêm POST_NOTIFICATIONS + BIND_NOTIFICATION_LISTENER_SERVICE. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />


    <queries>
        <!-- ========================================== -->
        <!-- I. NGÂN HÀNG THƯƠNG MẠI NHÀ NƯỚC (BIG 4)   -->
        <!-- ========================================== -->
        <!-- Vietcombank (VCB Digibank) -->
        <package android:name="com.VCB" /> <!-- Chú ý viết hoa VCB -->

        <!-- VietinBank iPay -->
        <package android:name="com.vietinbank.ipay" />

        <!-- BIDV SmartBanking -->
        <package android:name="com.vnpay.bidv" />

        <!-- Agribank Plus -->
        <package android:name="com.vnpay.Agribank3g" /> <!-- Chú ý viết hoa Agribank3g -->


        <!-- ========================================== -->
        <!-- II. NGÂN HÀNG THƯƠNG MẠI CỔ PHẦN TƯ NHÂN   -->
        <!-- ========================================== -->
        <!-- Techcombank Mobile -->
        <package android:name="vn.com.techcombank.bb.app" />

        <!-- MB Bank -->
        <package android:name="com.mbmobile" />

        <!-- VPBank NEO -->
        <package android:name="com.vnpay.vpbankonline" />

        <!-- TPBank Mobile -->
        <package android:name="com.tpb.mb.gprsandroid" />

        <!-- ACB ONE -->
        <package android:name="mobile.acb.com.vn" />

        <!-- VIB (MyVIB 2.0) -->
        <package android:name="com.vib.myvib2" />

        <!-- MSB mBank -->
        <package android:name="com.msb.digibank.retail" />

        <!-- OCB OMNI -->
        <package android:name="vn.com.ocb.awe" />

        <!-- LPBank -->
        <package android:name="vn.lienviet.app" />

        <!-- SACOMBANK mBanking & SACOMBANK Pay -->
        <package android:name="src.com.sacombank" />
        <package android:name="com.sacombank.ewallet" />


        <!-- ========================================== -->
        <!-- III. NGÂN HÀNG SỐ (DIGITAL / FOREIGN BANKS) -->
        <!-- ========================================== -->
        <!-- Shinhan Bank Vietnam -->
        <package android:name="com.shinhan.global.vn.bank" />

        <!-- Liobank (by OCB) -->
        <package android:name="com.ocb.liobank" />

        <!-- Cake by VPBank -->
        <package android:name="xyz.be.cake" />

        <!-- Digimi (by Bản Việt) -->
        <package android:name="vn.banvietbank.mobilebanking" />

        <!-- Timo Digital Bank -->
        <package android:name="io.lifestyle.plus" />


        <!-- ========================================== -->
        <!-- IV. VÍ ĐIỆN TỬ & TRUNG GIAN THANH TOÁN     -->
        <!-- ========================================== -->
        <!-- Ví MoMo -->
        <package android:name="com.mservice.momotransfer" />

        <!-- Ví ZaloPay -->
        <package android:name="vn.com.vng.zalopay" />

        <!-- Ví ShopeePay -->
        <package android:name="com.beeasy.toppay" /> <!-- Bản chính thức tại Việt Nam -->
        <package android:name="com.beeasy.airpay" /> <!-- Bản quốc tế / Thái Lan -->

        <!-- Ví VNPAY -->
        <package android:name="vnpay.smartacccount" /> <!-- Chú ý lỗi chính tả cố ý "acccount" -->

        <!-- Viettel Money -->
        <package android:name="com.viettelpay.android" /> <!-- Bản kế thừa gốc -->
        <package android:name="com.bplus.vtpay" /> <!-- Bản cập nhật mới -->
    </queries>

    <application
        android:name=".NotePayApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotePay"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="36">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.NotePay.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.NotePayNotificationListenerService"
            android:label="@string/notif_listener_service_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <!-- Tắt bộ khởi tạo mặc định của WorkManager -->
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
