# NotePay Project Rules

## Architecture Freeze

The current architecture is considered stable.

Agents must prefer extending existing modules over introducing new architectural layers.

Creating new repositories, new abstractions, new services, new use cases, or new packages requires explicit justification in the implementation plans.

**Reuse before rewrite.**
